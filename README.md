# RICMH 2026 — موقع المؤتمر (React + Vite)

```bash
npm install
npm run dev    # تطوير
npm run build  # إنتاج → مجلد dist/
```

- **`/`** = نفس موقع الـ HTML الكامل عبر iframe (`public/ricmh-legacy-full.html`).
- **5 صفحات فرعية** على React — انظر `MIGRATION.md`.
